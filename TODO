* Ruby 1.8 support
* JRuby support: use thread safe hash for worker jobs
* Worker:
  * better logging
  * restart after crashed
* timeout:
  * enq, deq, process
* retry on failure
  * enq, deq, process
* enqueue transaction?
